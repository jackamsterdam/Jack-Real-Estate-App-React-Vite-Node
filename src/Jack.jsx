// App for testing and my own information 

const Jack = () => {
    // to use global variables in VITE using the .env file: 
    // If you want to use in Vite the global variables: in create react app its process.env but in vite its import.meta.env.NAMEOFVARIABLE:
return <div>



{import.meta.env.VITE_API_URL} 
</div>
};

export default Jack

